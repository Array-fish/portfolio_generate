{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "local_generate": "npm run generate && cp -rf ./.output/public ./docs",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.6",
    "tailwindcss": "^3.1.6"
  },
  "dependencies": {
    "daisyui": "^2.19.1",
    "nuxi": "^3.0.0-rc.6"
  }
}
