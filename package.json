{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "move__nuxt": "mv ./.output/public/_nuxt ./.output/public/portfolio",
    "gp_generate": "npm run generate && npm run move__nuxt && mkdir -p ./docs && touch ./docs/.nojekyll && cp -rf ./.output/public/* ./docs",
    "start-static": "http-server -c-1 ./.output/public",
    "start-static-gp": "http-server -c-1 ./docs",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "nuxt": "3.0.0-rc.6",
    "tailwindcss": "^3.1.6"
  },
  "dependencies": {
    "daisyui": "^2.19.1",
    "nuxi": "^3.0.0-rc.6"
  }
}
